import styled from "styled-components/native";

import { DEFAULT_PADDING } from "@/components/layout-constants";

export const SafeHorizontalPadding = styled.View`
  padding-horizontal: ${DEFAULT_PADDING}px;
`;
