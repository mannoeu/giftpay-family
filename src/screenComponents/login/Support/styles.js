import styled from "styled-components/native";

export const Support = styled.View`
  align-items: center;
  gap: 4px;
`;

export const SupportLink = styled.TouchableOpacity.attrs({
  activeOpacity: 0.8,
})``;
